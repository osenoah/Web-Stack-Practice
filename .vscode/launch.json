{
    "configurations": [
            {
                "name": "Launch Chrome",
                "request": "launch",
                "type": "chrome",
                "timeout": 20000,
                "url": "${file}",
                "webRoot": "${workspaceFolder}"
        },
    ]
}